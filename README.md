# URLProtocol stubbing: a testing strategy for network request

A testing strategy that allows using URLProtocol to test a network request. URLProtocol stubbing decouples a test from production code. 
